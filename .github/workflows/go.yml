name: Go

on:
  workflow_dispatch:

  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Check out code into the Go module directory
        uses: actions/checkout@v5

      - name: Set up Go 1.x
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          go-version-file: 'server/go.mod'
          cache-dependency-path: 'server/go.sum'

      - name: Get dependencies
        run: |
          cd server
          go get -v -t -d ./...

      - name: Build
        run: |
          cd server
          mkdir ui
          touch ui/index.html
          go build -v -o remlink -trimpath -ldflags "-X main.CommitId=`git rev-parse HEAD`"
          ./remlink tool -v

      - name: Test
        run: |
          cd server
          go test ./...
          go test -race -coverprofile=coverage.txt -covermode=atomic -v ./...
